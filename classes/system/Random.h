#ifndef randomH
#define randomH

#include <stdlib.h>

unsigned int RandomChance(unsigned int n);
unsigned int RandomChancePercent(unsigned int n);
int RandomValue(int Min, int Max);
int RamdomFromCount2(int Count, int& RetVal1, int& RetVal2);

#endif
